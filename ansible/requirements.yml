---
# ansible-galaxy collection install -r requirements.yml
# ansible.posix 2.0+ requires ansible-core 2.15+; pin <2.0.0 for core 2.14
collections:
  - name: ansible.posix
    version: ">=1.5.0,<2.0.0"
